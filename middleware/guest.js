// import _get from 'lodash/get';

// export default ({ app, redirect }) => {
//     const authenticateUser = _get(app, '$state.loggedIn', null);
//     if (authenticateUser) {
//         console.log('Authentication',authenticateUsers)
//         redirect('/');
//     }
// };